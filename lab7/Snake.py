import pygame
WIDTH = 720
HEIGHT = 720
screen = pygame.display.set.mode((WIDTH,HEIGHT))

FPS = 10

class Point:
    def_init_(self,x,y) 

    self.x = x
    self.y=yield
    def generate():
        pass
    def draw_grid();

while not done:
    for event in pygame.get():
        if event.type == pygame.QUIT:
            done = True